---
title: Per-chapter timing sheet
description: Cohort cadence view built from lab frontmatter (facilitator-only).
classification: "Cisco Confidential"
---

# Per-chapter timing sheet

Durations are **generated at build time** from each lab chapter’s `estimated_duration` (AR14). Rebuild the instructor site after editing chapter frontmatter; no manual edits to this page are required.

This page is **HTML-only in v1.0** — print from the browser if you need a hardcopy. PDF export for facilitator artifacts is **Phase 2** (same as the [proctor checklist](proctor-checklist.md)).

{{ timing_sheet() }}
